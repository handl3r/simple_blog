<%= form_with model: current_user.given_follows.build, local: true do |f| %>
  <div><%= hidden_field_tag :followed_user_id, @user.id %></div>
  <%= f.submit "Follow", class: "follow_button" %>
<% end %>